================================================================================
PLACEIIT APPLICATION - COMPLETE API & UNIT TESTING SPECIFICATION (CURRENT CODEBASE)
================================================================================

1. EXECUTIVE SUMMARY
--------------------------------------------------------------------------------
This documentation provides extensive Postman automation scripts and Unit Testing 
logic mappings exclusively constructed from the real, current internal application 
codebase constraints (bypassing deprecated ideas like priority-ordering).

================================================================================
PART A: AUTOMATED API TESTING (POSTMAN SCRIPTS)
================================================================================
Setup variables in Postman Environment: `{{baseUrl}}`, `{{studentToken}}`, `{{cocoToken}}`, `{{apcToken}}`.

--------------------------------------------------------------------------------
1. STUDENT PORTAL
--------------------------------------------------------------------------------

Feature 1.1: Fetch Profile & Resume
Endpoint: GET /api/v1/student/profile
Type: GET
Headers: Authorization: Bearer {{studentToken}}
Postman Script:
```javascript
pm.test("Status code is 200", function () { pm.response.to.have.status(200); });
pm.test("Loads Profile Data", function () {
    var js = pm.response.json();
    pm.expect(js.name).to.exist;
    pm.expect(js.rollNumber).to.exist;
});
```

Feature 1.2: Get Active Shortlisted & Allowed Walk-in Companies
Endpoint: GET /api/v1/student/companies
Type: GET
Headers: Authorization: Bearer {{studentToken}}
Postman Script:
```javascript
pm.test("Fetched Companies list", function() { pm.response.to.have.status(200); });
pm.test("Returns Array of Company UI Tiles", function() {
    // Assert array is returned representing the tiles
    pm.expect(pm.response.json()).to.be.an('array');
});
```

Feature 1.3: Join Standard Interview Queue (Drive State Locked)
Description: Joins a company queue. Validates against global DriveState (Day/Slot).
Endpoint: POST /api/v1/student/queue/join
Type: POST
Valid Input: { "companyId": "cmp_123", "round": "Round 1" }
Invalid Input: Joining a company that is not matching current DriveState Day/Slot (Returns 403)
Postman Script:
```javascript
pm.test("Handled Join Correctly (200, 201 or 409)", function () { 
    pm.response.to.be.oneOf([200, 201, 403, 409]); 
});
```

Feature 1.4: Leave Queue or Switch Context
Endpoint: POST /api/v1/student/queue/leave
Type: POST
Valid Input: { "companyId": "id" }

Endpoint: POST /api/v1/student/queue/confirm-switch
Type: POST
Valid Input: { "fromCompanyId": "id1", "toCompanyId": "id2" }

Feature 1.5: Helpdesk (Query Submission)
Endpoint: POST /api/v1/student/queries
Type: POST
Valid Input: { "subject": "Missing Slot", "message": "Can't find company." }

--------------------------------------------------------------------------------
2. COCO (COORDINATOR) PORTAL
--------------------------------------------------------------------------------

Feature 2.1: Native Assigned Company Loading
Endpoint: GET /api/v1/coco/company
Type: GET
Postman Script:
```javascript
pm.test("Loads Assigned Company correctly", function () { pm.response.to.have.status(200); });
```

Feature 2.2: Toggle Walk-in Allowance
Endpoint: PUT /api/v1/coco/company/:companyId/walkin
Type: PUT
Valid Input: { "allowWalkIn": true }

Feature 2.3: Add Round Tracking & Panels
Endpoint: POST /api/v1/coco/round
Type: POST
Valid Input: { "roundName": "Interview Round" }

Endpoint: POST /api/v1/coco/panel
Type: POST
Valid Input: { "panelName": "Technical Panel A" }

Feature 2.4: Queue Status Modifiers (Accept, Reject, Complete)
Endpoint: PUT /api/v1/coco/queue/status
Type: PUT
Valid Input: { "studentId": "uid", "queueId": "qid", "status": "IN_INTERVIEW" }
Postman Script:
```javascript
pm.test("Updates Student specific queue stage", function () { pm.response.to.have.status(200); });
```

Feature 2.5: Realtime Student Searching
Endpoint: GET /api/v1/coco/students/search?q=19011
Type: GET

--------------------------------------------------------------------------------
3. APC (ADMIN) PORTAL
--------------------------------------------------------------------------------

Feature 3.1: Global SubSystem Analytics
Endpoint: GET /api/v1/admin/stats
Type: GET
Postman Script:
```javascript
pm.test("Stats loaded successfully", function () { 
    var js = pm.response.json();
    pm.expect(js.students).to.be.a('number');
    pm.expect(js.companies).to.be.a('number');
});
```

Feature 3.2: Manage Drive State (Day & Slot Engine)
Endpoint: PUT /api/v1/admin/drive-state
Type: PUT
Valid Input: { "currentDay": 1, "currentSlot": "Morning" }
Postman Script:
```javascript
pm.test("Global Drive engine state updated successfully", function () { pm.response.to.have.status(200); });
```

Feature 3.3: Automated Coco Allocation Engine (Random Assigner)
Endpoint: POST /api/v1/admin/auto-allocate-cocos
Type: POST
Postman Script:
```javascript
pm.test("Executes Random Shuffling Assignment Algo", function () { pm.response.to.have.status(200); });
```

Feature 3.4: Manage Entities Creation & Roles
- POST `/api/v1/admin/students` (Add Student)
- POST `/api/v1/admin/cocos` (Add Coco)
- POST `/api/v1/admin/apc` (Add APC)

Feature 3.5: Master Excel System (Drop Zones parsing)
Endpoint: POST /api/v1/admin/upload/companies (Returns populated Company IDs)
Endpoint: POST /api/v1/admin/upload/shortlist
Endpoint: POST /api/v1/admin/upload/students
Endpoint: POST /api/v1/admin/upload/cocos
Type: POST (`multipart/form-data`)
Valid Payload: Key -> `file`, Value -> `.xlsx`
Postman Script:
```javascript
pm.test("Uploaded File Successfully parsed", function () { pm.response.to.have.status(200); });
```

Feature 3.6: Fetching Resolvable Queries globally
Endpoint: GET /api/v1/admin/queries 
Endpoint: PUT /api/v1/admin/queries/:id (Resolve)


================================================================================
PART B: INTERNAL UNIT TESTING SCRIPTS (NODE.JS / JEST)
================================================================================

1. UNIT TESTS: STUDENT MODULE
--------------------------------------------------------------------------------
```javascript
describe("Student Capabilities", () => {

  describe("Drive State Lock Enforcement", () => {
    it("should BLOCK a student (403) from joining a queue if the Company Day/Slot does NOT match Admin's active Drive State", async () => {
         // Setup: Drive: Day 1, Slot A. Company Y is Day 2, Slot B.
         // Action: Student calls Join Queue for Company Y.
         // Assert: Response is 403 Forbidden with lock notification.
    });
    
    it("should ALLOW queue join if Company Day/Slot exactly equals Drive State", async () => {
         // Assert correctly enters Queue and sets Status to PENDING/IN_QUEUE.
    });
  });

  describe("Queue Operations (Switching/Leaving)", () => {
    it("should correctly update previous queue status to EXITED and insert new PENDING status upon Switch", async () => {
         // Trigger switch -> View Queue Database mapping to guarantee integrity.
    });
  });
});
```

2. UNIT TESTS: COCO MODULE
--------------------------------------------------------------------------------
```javascript
describe("Coordinator Event Tracking", () => {
  
  describe("Company Environment Management", () => {
    it("should correctly toggle `allowWalkin` state for mapped company", async () => {
        // Toggle True -> Assert DB Company.isWalkInEnabled == true
    });
  });

  describe("Dynamic Tracking Updates", () => {
    it("should update a students exact tracker status (IN_INTERVIEW) and broadcast sockets", async () => {
        // Expect Queue collection 'status' to modify to new Enum smoothly.
    });

    it("should correctly map newly generated Interview Panels directly to the company root ID", async () => {
        // Add panel. Check reference links.
    });
  });
});
```

3. UNIT TESTS: APC (ADMIN) MODULE
--------------------------------------------------------------------------------
```javascript
describe("APC Global Administration", () => {
  
  describe("Automated Coco Allocation Algorithm", () => {
    it("should randomly assign all unallotted companies to a random Coordinator without Day/Slot overlap conflicts", async () => {
        // Mock 10 companies across 3 slots and 15 Cocos.
        // Run -> Assert 'assignedCocos' on Companies is populated. 
        // Iterate -> Ensure Coco assignments never have identical Day && Slot.
    });
    
    it("should securely track unassignable components when shortage of Coordinators exist", async () => {
        // Provide 10 companies in Day 1 Slot 1 but only 5 Cocos exist. 
        // Run -> Assert HTTP Response lists 5 Unallotted warnings cleanly.
    });
  });

  describe("File System Data Ingestion (Excel)", () => {
    it("should decode complex .xlsx spreadsheets and inject data natively into Database", async () => {
        // Stub multer/fs data stream mimicking an upload of 5 Students.
        // Run Student Upload processing function.
        // Assert Student collection grows by +5.
    });
  });

  describe("Drive States Engine", () => {
    it("should forcefully mutate live DriveState allowing students into new phase segments", async () => {
        // PUT API -> Assert new schema config globally reflects Day 2 Slot B.
    });
  });
});
```

================================================================================
CHECKLIST OF CHANGES INTEGRATED (BASED ON CODEBASE)
================================================================================
- [x] Eliminated Priority-Order mechanisms and assumptions completely.
- [x] Incorporated the core 'Drive-State' Lockout logic implemented in Queues.
- [x] Incorporated Queries (Helpdesk), Excel Data Pipelines, and Socket Triggers.
- [x] Updated Auto Allocator Logic testing (Handling Conflict constraints).
